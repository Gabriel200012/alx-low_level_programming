#include "main.h"
/**
 * main: to write a function that adds two integers and returns the result.
 * @a: int type number
 * @b: int type number
 * Return:0
 */

int add(int a, int b)
{
return (a + b);
}
