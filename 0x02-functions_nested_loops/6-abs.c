#include "main.h"

/**
 * main : Write a function that computes the absolute value of an integer.
 * _abs - return abs value
 * @n : number to check
 * Return:0 or 1
 */

int _abs(int n)
{
return (n >= 0 ? n : n * -1);
}
